package com.textparser.textparser;

import org.springframework.context.annotation.*;


@Configuration
@ComponentScan("com.textparser.textparser")
@PropertySource("classpath:application.properties")
public class AppConfig {
}